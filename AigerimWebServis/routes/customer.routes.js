module.exports = app => {
  const customers = require("../controllers/customer.controller.js");

  // app.post("/customers", customers.create);
  //app.get("/customersAll", customers.findAll);
 // app.get("/customers/:customerId", customers.findOne);
 // app.get("/Airjin/customersPNCount/:bastar/:bittar", customers.CustomerCounts);
  //app.get("/Analizm/customersPNCount/:testcode/:bastar/:bittar", customers.Analizm);
 

};